apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "synapse-matrix.fullname" . }}-test-connection"
  labels:
    {{- include "synapse-matrix.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "synapse-matrix.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
